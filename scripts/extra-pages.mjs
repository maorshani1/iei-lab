export function extraPages({page,intro,campusPhoto,H,E,ext,button,media,conferences,campus,published}){
const filter=(id,items,label,types)=>`<div class="filter-panel"><div class="control grow"><label for="${id}-query">Search ${label}</label><input type="search" id="${id}-query" data-filter-search placeholder="Topic, outlet, place, or title"></div><div class="control"><label for="${id}-year">Year</label><select id="${id}-year" data-filter-year><option value="">All years</option>${[...new Set(items.map(x=>x.year).filter(Boolean))].sort((a,b)=>b-a).map(y=>`<option>${y}</option>`).join('')}</select></div><div class="control"><label for="${id}-type">Type</label><select id="${id}-type" data-filter-topic><option value="">All types</option>${types.map(t=>`<option>${E(t)}</option>`).join('')}</select></div><button class="ghost-button" data-reset-filters>Reset</button></div><p data-filter-count class="small" aria-live="polite"></p>`;
page('media','Media and public scholarship','Interviews, research coverage, commentary and community publications.',()=>`${intro('Public engagement','Media and public scholarship','Interviews, coverage of research, and public discussions of identity, antisemitism, hope, and health.')}<section class="container">${campusPhoto('campus-architecture',{className:'campus-wide',link:'conferences'})}<div class="notice">An archival selection based on the supplied media list and academic CV, with selected links checked in September 2026. External authors, institutional coverage, and print-only entries are identified separately. This is not a complete or continuously updated press list.</div><div data-filter-scope data-record-label="media records">${filter('media',media,'the media archive',[...new Set(media.map(x=>x.category))])}${media.map(m=>`<article class="archive-record" data-filter-item data-year="${m.year||''}" data-topic="${E(m.category)}" data-search="${E([m.title,m.outlet,m.category,m.topic,m.language,m.description].join(' '))}" id="${m.id}"><div class="archive-meta">${E(m.outlet)} · ${E(m.date||m.year||'Date not confirmed')} · ${E(m.language)} · ${E(m.category)}</div><h3>${m.url?ext(m.url,E(m.title)):E(m.title)}</h3><p>${E(m.description)}</p><p class="small muted">${E(m.verification)}</p></article>`).join('')}<p data-empty hidden>No media entries match this selection.</p></div><div class="actions" style="margin:35px 0 60px">${button('contact','Media and speaking inquiries',true)}</div></section>`);
page('conferences','Conferences and invited talks','Conference presentations and invited lectures from Maor Shani’s academic CV.',()=>`${intro('Academic activities','Conferences and invited talks','Presentations on antisemitism, intergroup relations, political psychology, and health.')}<section class="container">${campusPhoto('campus-lower',{className:'campus-wide',link:'about'})}<p class="source-line">Archive transcribed from the academic CV dated 10 September 2026: 26 conference presentations and four invited lectures. Dates and event names follow that record and have not all been checked against organizer programs. No upcoming appearance is implied.</p><div data-filter-scope data-record-label="conference and talk records">${filter('events',conferences,'presentations',['Conference presentation','Invited lecture'])}${conferences.map(c=>`<article class="archive-record" data-filter-item data-year="${c.year}" data-topic="${E(c.type)}" data-search="${E(c.citation+' '+c.topic)}" id="${c.id}"><div class="archive-meta">${c.year} · ${E(c.type)} · ${E(c.topic)}</div><p class="archive-citation">${E(c.citation)}</p></article>`).join('')}<p data-empty hidden>No events match this selection.</p></div><p class="small" style="margin:35px 0 65px">For invitations or presentation materials, use the <a href="${H('contact')}">contact page</a>. Materials are not linked unless a public source has been verified.</p></section>`);
page('ask','Ask the lab','A source-based guide to the lab’s research, supervision and public activities.',()=>`${intro('Website guide','Ask the lab','Find an answer and the relevant pages without searching the whole website.')}<section class="container lab-guide" id="lab-guide"><div class="notice"><strong>Source-based guide, not generative AI.</strong> This version matches your question to curated answers and website sources. It does not invent answers, access private Drive files, or send your question to an AI provider.</div><form><label for="guide-question" class="sr-only">Your question about the lab</label><input id="guide-question" maxlength="500" placeholder="What does the lab study?" required autocomplete="off"><button class="button" type="submit">Find an answer</button></form><div class="guide-prompts">${['How do I ask about thesis supervision?','Where can I explore correlations?','What conferences has Maor presented at?','Where are the media interviews?'].map(q=>`<button class="ghost-button" data-guide-question="${E(q)}">${E(q)}</button>`).join('')}</div><div data-guide-answer aria-live="polite" style="margin:35px 0 70px"></div><p class="small">This guide cannot confirm vacancies, give clinical advice, disclose unpublished data, or speak on behalf of the university. Direct questions that require a personal response to <a href="${H('contact')}">Maor</a>.</p></section>`);
page('data','Research data explorer','Explore all published correlations among ten variables in the Germany antisemitism study.',()=>`${intro('Research data','Explore associations between variables','Select a study, choose any two included variables, or open the full correlation matrix.')}<section class="container"><div class="notice">This public version includes the ten-variable published Germany matrix. It is based on reported aggregate statistics, not simulated participants. Unpublished experimental datasets are not included in this public build.</div><div id="data-lab"></div><script type="application/json" id="data-lab-json">${JSON.stringify(published).replace(/</g,'\\u003c')}</script></section>`);
page('image-credits','Image credits and sources','Sources and licenses for campus photographs and publication previews.',()=>`${intro('Image sources','Image credits','Campus photographs are distinguished from AI-generated editorial illustrations and actual publication-page previews.')}<section class="container prose"><h2>Ariel University campus photographs</h2><p>These are documentary photographs of university buildings, not images of the IEI Lab or evidence that the lab occupies the buildings shown. Dates refer to the source photographs. Images are displayed at a different size and may be cropped in the layout. Each photograph retains its stated license; this does not license the entire website.</p>${campus.map(c=>`<article class="archive-record"><h3>${E(c.title)} (${c.year})</h3><p>${E(c.credit)} · ${ext(c.licenseUrl,E(c.license))}</p><p>${ext(c.source,'Original photograph and attribution')}</p></article>`).join('')}<h2>Publication previews</h2><p>Small previews are rendered from actual publication pages or the author-supplied report. Clicking the citation opens the journal, publisher, or institutional source. Previews do not replace the full publication, and copyright remains with the relevant authors and publishers. A missing preview is identified rather than replaced by a fabricated cover.</p><h2>AI-generated editorial illustrations</h2><p>The desk, research, and mentoring illustrations from the earlier website version remain explicitly labeled. They do not show actual lab members, participants, or study findings.</p><h2>Remote-image privacy</h2><p>When a campus image has not been cached with the site, your browser loads it from Wikimedia’s image host. That host receives ordinary request information such as your IP address. No tracking query parameters are added. The deployment preparation script can cache these photographs locally.</p></section>`);
}
